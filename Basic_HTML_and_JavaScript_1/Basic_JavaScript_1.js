






/*
var sum = add(5,7);
function add(num1,num2) {
	return num1 + num2;
}

student.gradeAverage(3.4);

// Note that a function is just a set of instructions that can be used when called.
// A method is a function that an OBJECT can perform.  Ex:  Above, the student used the gradeAverage method.

var student = {
	firstname: "Jane";
	lastName: "Smith";
	age: 28;
	gradeAverage: function(avg) { //This function needs and input.  This function is a method
		return avg;
	}
}; //Notic the semicolon

document.getElementById("Header_1").innerHTML */